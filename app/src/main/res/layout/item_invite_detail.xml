<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="4dp"
    android:background="#fff">

    <ImageView
        android:id="@+id/img_item_invite_detail_avatar"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_centerVertical="true"
        android:layout_marginRight="15dp"
        android:src="@color/colorAccent" />

    <TextView
        android:id="@+id/img_item_invite_detail_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/img_item_invite_detail_avatar"
        android:layout_toRightOf="@+id/img_item_invite_detail_avatar"
        android:text="高原"
        android:textColor="@color/zy_text_gray"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/tv_invit_detail_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/img_item_invite_detail_avatar"
        android:layout_toRightOf="@+id/img_item_invite_detail_avatar"
        android:text="2017.5.21"
        android:textColor="@color/zy_text_gray"
        android:textSize="12sp" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/img_item_invite_detail_state"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="接受"
            android:textSize="16sp" />

        <ImageView
            android:id="@+id/img_item_invite_detail_chat"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginLeft="15dp"
            android:layout_marginRight="15dp"
            android:src="@mipmap/chat_blue" />
    </LinearLayout>

</RelativeLayout>
